import { Typography } from '@mui/material';
import React from 'react'

function HeaderForm () {
  return (
    <div>
        <Typography variant='h5' className="font-bold"> Contact Form </Typography>
    </div>
  )
}

export default HeaderForm;